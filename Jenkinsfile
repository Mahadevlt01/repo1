pipeline {
    agent any

    stages {
        stage("git checkout maven pom file"){
            steps{
                
            }
        }
        stage("Maven Build"){
            steps{
                sh "mvn package"
            }
        }
    }
}
